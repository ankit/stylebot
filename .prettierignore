dist
firefox-dist
node_modules
coverage
test-results
playwright-report
storybook-static
junit.xml
patches
yarn.lock
CHANGELOG.md

# Separate Gatsby project with its own toolchain.
docs

# Prompt content — the manual line wrapping there is deliberate.
.claude

# Chrome profile created by yarn dev:chrome.
.chrome-dev-profile
