export const SYNC_FOLDER_NAME = 'stylebot';
export const SYNC_FILE_NAME = 'stylebot_v3_backup.json';

// Shown to the user so they know exactly what sync writes to their Drive.
export const SYNC_FILE_PATH = `${SYNC_FOLDER_NAME}/${SYNC_FILE_NAME}`;
