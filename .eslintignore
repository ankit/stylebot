dist
firefox-dist
node_modules
coverage
test-results
playwright-report
patches

# Separate Gatsby project with its own toolchain.
docs

# Playwright transpiles these with esbuild, so they use TS/ES syntax (`??=`,
# inline `type` import specifiers) that the pinned typescript@3.9 the parser
# reads ASTs through cannot handle. Lintable once TypeScript is on 4+.
e2e
